# Tasks – Project_Status_Browser (current pointer)

Canonical source: `tasks/projects/Project_Status_Browser/sessions/session-1765832163/tasks.md`. This file keeps the required phase markers for governance and points to the session task list.

## Phase 0 – Foundations (Data Model & Storage)
- See session tasks for detailed steps: types/schema, status_pages storage, backend validation.

## Phase 1 – Core Behavior (View Route & Renderer)
- See session tasks for GET /api/status-pages/:slug, renderer/registry, `/status/:slug` route.

## Phase 2 – Editor & Save/Load
- See session tasks for PUT /api/status-pages/:slug, StatusPageEditor shell, `/status/:slug/edit` with auth.

## Phase 3 – Layout & Slots
- See session tasks for Grid/Column layouts, slot editing/reorder in editor.

## Phase 4 – Inline Editing & UX Polishing
- See session tasks for inline text editing and selection overlay patterns.

## Phase 5 – NFRs / Hardening
- See session tasks for API contract tests, performance checks, migration strategy.
